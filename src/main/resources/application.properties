spring.application.name=jcclub
server.port=8081

# 리버스 프록시(nginx) 뒤에서 X-Forwarded-For를 실제 클라이언트 IP로 사용 (레이트 리밋용)
server.forward-headers-strategy=framework

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# 계정과 앱 비밀번호는 저장소에 두지 않는다. 서버의 config/application.properties 참고
spring.mail.username=
spring.mail.password=

quote.mail.to=zzin0kim@gmail.com
# Gmail은 인증 계정 외의 발신자를 허용하지 않아 From을 계정으로 덮어쓴다
quote.mail.from=${spring.mail.username}
